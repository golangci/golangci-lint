version: "2"
linters:
  settings:
    iface:
      enable:
        - identical
        - unused
        - opaque
      settings:
        unused:
          exclude:
            - github.com/example/log
