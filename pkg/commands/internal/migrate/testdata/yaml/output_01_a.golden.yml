version: "2"
output:
  formats:
    text:
      path: stdout
    json:
      path: stderr
    tab:
      path: stdout
    html:
      path: stdout
    checkstyle:
      path: report.xml
    code-climate:
      path: stdout
    junit-xml:
      path: stdout
      extended: true
    teamcity:
      path: stdout
    sarif:
      path: stdout
