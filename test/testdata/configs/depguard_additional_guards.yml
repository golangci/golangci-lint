linters-settings:
  depguard:
    include-go-root: true
    packages: 
      - compress/*
    packages-with-error-message:
      log: "don't use log"
    additional-guards:
      - include-go-root: true
        packages:
          - fmt
        packages-with-error-message:
          strings: "disallowed in additional guard"

