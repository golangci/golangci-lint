version: "2"

linters-settings:
  forbidigo:
    forbid:
      - fmt\.Print.*
      - time.Sleep(# no sleeping!)?

linters:
  exclusions:
    rules:
      # Apply forbidigo only to test files, exclude
      # it everywhere else.
      - path-except: _test\.go
        linters:
          - forbidigo
